# Benchmark harness dependencies (run manually; not part of the library build).
pymupdf4llm>=0.0.17
rapidfuzz>=3.6         # fast normalized edit distance on long documents
apted>=1.0.3          # tree edit distance for TEDS
requests>=2.31        # download GT (HuggingFace) + PDFs (arXiv)
reportlab>=4.0        # only needed to (re)generate the synthetic report.pdf fixture
